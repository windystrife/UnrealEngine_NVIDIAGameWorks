INTSourceChangelist:3108692
Title: DependsOn
Crumbs:%ROOT%, Programming, Programming/UnrealArchitecture/Reference, Programming/UnrealArchitecture/Reference/Classes, Programming/UnrealArchitecture/Reference/Classes/Specifiers
Description:
Availability:Public
Version: 4.9

이 클래스보다 *ClassName* 이 먼저 컴파일됨을 나타냅니다. *ClassName* 은 같은 (또는 예전) 패키지에 있는 클래스를 지정해야 합니다. 의존 클래스가 여러개 있는 경우, `DependsOn` 한 줄에 쉼표로 구분해서 지정하거나, 각 클래스마다 별도의 `DependsOn` 줄을 사용해서 지정해도 됩니다. 이는 한 클래스가 다른 클래스에 선언된 구조체 또는 Enum 을 사용할 때 중요한데, 컴파일러는 이미 컴파일된 클래스에 있는 것만 알기 때문입니다.

DependsOn=(ClassName, Classname, ...)