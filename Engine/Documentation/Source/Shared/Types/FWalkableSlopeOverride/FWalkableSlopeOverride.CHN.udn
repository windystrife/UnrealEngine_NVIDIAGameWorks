INTSourceChangelist:2574161
Availability:NoPublish
Title:FWalkableSlopeOverride RTTs
Crumbs:

[EXCERPT:WalkableSlopeBehavior]

确认可行走的斜坡角度如何在已存的可行走角度上运行，以及其是增大还是减少。
[/EXCERPT:WalkableSlopeBehavior]



[EXCERPT:WalkableSlopeAngle]

该值在现存的可行走角度上运行，根据由可行走斜坡设置的方式进行修改。
如果增大可行走斜坡，这样仅能增大可行走性，而不会将角色限制于比普通情况更低的斜坡上。
如果减少可行走斜坡，这样仅能降低可行走性，而不会让角色穿过比普通情况更陡的斜坡。
[/EXCERPT:WalkableSlopeAngle]
