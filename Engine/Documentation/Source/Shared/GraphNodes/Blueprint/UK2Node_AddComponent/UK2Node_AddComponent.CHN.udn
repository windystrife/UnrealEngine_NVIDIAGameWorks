INTSourceChangelist:3108692
Availability:NoPublish
Title:UK2Node_AddComponent
Crumbs:
Description:蓝图“Add Component”节点总览。

[EXCERPT:ArrowComponent]

ArrowComponent 是一个简单的箭头形状渲染使用线（用于指出物体朝向的对象）。执行时，此节点将为特定的“Target”Actor 新建一个 ArrowComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 ArrowComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:ArrowComponent]

[EXCERPT:AtmosphericFogComponent]

AtmosphericFogComponents 用于创建雾气效果（如云朵）。执行时，此节点将为特定的“Target”Actor 新建一个 AtmosphericFogComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 AtmosphericFogComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:AtmosphericFogComponent]

[EXCERPT:AudioComponent]

AudioComponents 用于提供带音频的对象。执行时，此节点将为特定的“Target”Actor 新建一个 AudioComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 AudioComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:AudioComponent]

[EXCERPT:BoxComponent]

BoxComponent 是一个简单的方块形渲染使用线（通常用于简单碰撞）。执行时，此节点将为特定的“Target”Actor 新建一个 BoxComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 BoxComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:BoxComponent]

[EXCERPT:CapsuleComponent]

CapsuleComponent 是一个简单的胶囊形渲染使用线（通常用于简单碰撞）。执行时，此节点将为特定的“Target”Actor 新建一个 CapsuleComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 CapsuleComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:CapsuleComponent]

[EXCERPT:CustomMeshComponent]

CustomMeshComponent 可用于生成自定义三角形模型几何体。执行时，此节点将为特定的“Target”Actor 新建一个 CustomMeshComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 CustomMeshComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:CustomMeshComponent]

[EXCERPT:DecalComponent]

DecalComponent 是渲染到模型表面的材质（就像是模型的“贴纸”一样）。执行时，此节点将为特定的“Target”Actor 新建一个 DecalComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 DecalComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:DecalComponent]

[EXCERPT:DestructibleComponent]

DestructibleComponent 保存 DestructibleActor 的物理数据。执行时，此节点将为特定的“Target”Actor 新建一个 DestructibleComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 DestructibleComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:DestructibleComponent]

[EXCERPT:DirectionalLightComponent]

DirectionalLightComponent 可形成类似于太阳的光源。它在全局光照重要性体积域中的表面上形成均匀的光照。执行时，此节点将为特定的“Target”Actor 新建一个 DirectionalLightComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 DirectionalLightComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:DirectionalLightComponent]

[EXCERPT:ExponentialHeightFogComponent]

ExponentialHeightFogComponent 与 AtmosphericFogComponent 相似（用于创建云朵之类的雾气效果），不同点是其带有和雾气高度相关的密度。执行时，此节点将为特定的“Target”Actor 新建一个 ExponentialHeightFogComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 ExponentialHeightFogComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:ExponentialHeightFogComponent]

[EXCERPT:InstancedStaticMeshComponent]

InstancedStaticMeshComponent 是一个可拥有多个实例的静态网格体。执行时，此节点将为特定的“Target”Actor 新建一个 InstancedStaticMeshComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 InstancedStaticMeshComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:InstancedStaticMeshComponent]

[EXCERPT:MaterialBillboardComponent]

UMaterialBillboardComponent 是一个 2D 材质，此材质被渲染时将固定朝向摄像机。执行时，此节点将为特定的“Target”Actor 新建一个 UMaterialBillboardComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 UMaterialBillboardComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:MaterialBillboardComponent]

[EXCERPT:ParticleSystemComponent]

ParticleSystemComponent 的作用是粒子发射器。执行时，此节点将为特定的“Target”Actor 新建一个 ParticleSystemComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 ParticleSystemComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:ParticleSystemComponent]

[EXCERPT:PawnNoiseEmitterComponent]

PawnNoiseEmitterComponent 追踪 PawnSensingComponents 使用的 noise 事件数据。此组件存在于 Pawn 或其控制器上。它不在网络客户端上执行任何操作。 

执行时，此节点将为特定的“Target”Actor 新建一个 PawnNoiseEmitterComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 PawnNoiseEmitterComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:PawnNoiseEmitterComponent]

[EXCERPT:PawnSensingComponent]

PawnSensingComponent 封装 Actor 的感知（即视觉和听觉）设置和功能，使 Actor 看见/听见世界场景中的 Pawn。它不在网络客户端上执行任何操作。 

执行时，此节点将为特定的“Target”Actor 新建一个 PawnSensingComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 PawnSensingComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:PawnSensingComponent]

[EXCERPT:PhysicsConstraintComponent]

PhysicsConstraintComponent 实际上是一个连接点，可将两个刚体连接到一起。可使用此组件的诸多参数创建不同类型的连接点。

执行时，此节点将为特定的“Target”Actor 新建一个 PhysicsConstraintComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 PhysicsConstraintComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:PhysicsConstraintComponent]

[EXCERPT:PhysicsHandleComponent]

PhysicsHandleComponents 是移动物理 Actor 的实用对象。执行时，此节点将为特定的“Target”Actor 新建一个 PhysicsHandleComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 PhysicsHandleComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:PhysicsHandleComponent]

[EXCERPT:PhysicsThrusterComponent]

PhysicsThrusterComponents 是移动物理 Actor 的实用对象。执行时，此节点将为特定的“Target”Actor 新建一个 PhysicsThrusterComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 PhysicsThrusterComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:PhysicsThrusterComponent]

[EXCERPT:PointLightComponent]

PointLightComponents 是从一个单点向所有方向发射灯光的光照组件。执行时，此节点将为特定的“Target”Actor 新建一个 PointLightComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 PointLightComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:PointLightComponent]

[EXCERPT:PoseableMeshComponent]

PoseableMeshComponent 允许骨骼变形通过蓝图进行驱动。执行时，此节点将为特定的“Target”Actor 新建一个 PoseableMeshComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 PoseableMeshComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:PoseableMeshComponent]

[EXCERPT:ProjectileMovementComponent]

ProjectileMovementComponents 在其 tick 中更新相关组件的位置。如更新组件模拟物理，只有最初的发射参数（当初速度为非零时）会影响发射物，物理模拟将从此处开始执行。

执行时，此节点将为特定的“Target”Actor 新建一个 ProjectileMovementComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 ProjectileMovementComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:ProjectileMovementComponent]

[EXCERPT:RadialForceComponent]

RadialForceComponents 用于发射一个径向力/脉冲，能够对物理对象和/或可破坏对象产生影响。执行时，此节点将为特定的“Target”Actor 新建一个 RadialForceComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 RadialForceComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:RadialForceComponent]

[EXCERPT:RotatingMovementComponent]

RotatingMovementComponents 用于更新每个 tick 时其相关 PrimitiveComponent 的位置。执行时，此节点将为特定的“Target”Actor 新建一个 RotatingMovementComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 RotatingMovementComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:RotatingMovementComponent]

[EXCERPT:SceneCaptureComponent2D]

SceneCaptureComponent2D 可用于从一个单一平面捕捉场景的“快照”，并将其填入渲染目标。 

执行时，此节点将为特定的“Target”Actor 新建一个 SceneCaptureComponent2D。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 SceneCaptureComponent2D 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:SceneCaptureComponent2D]

[EXCERPT:SceneCaptureComponentCube]

SceneCaptureComponentCube 可用于从一个单一平面捕捉场景的“快照”，并将其填入渲染目标。 

执行时，此节点将为特定的“Target”Actor 新建一个 SceneCaptureComponentCube。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 SceneCaptureComponentCube 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:SceneCaptureComponentCube]

[EXCERPT:SceneComponent]

执行时，此节点将为特定的“Target”Actor 新建一个 SceneComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 SceneComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:SceneComponent]

[EXCERPT:SkeletalMeshComponent]

执行时，此节点将为特定的“Target”Actor 新建一个 SkeletalMeshComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 SkeletalMeshComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:SkeletalMeshComponent]

[EXCERPT:SphereComponent]

SphereComponent 是一个简单的球形渲染使用线（通常用于简单碰撞）。执行时，此节点将为特定的“Target”Actor 新建一个 SphereComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 SphereComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:SphereComponent]

[EXCERPT:SpotLightComponent]

SpotLightComponent 发射定向圆锥形灯光（如火把）。执行时，此节点将为特定的“Target”Actor 新建一个 SpotLightComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 SpotLightComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:SpotLightComponent]

[EXCERPT:DecalComponent]

DecalComponent 是渲染到模型表面的材质（就像是模型的“贴纸”一样）。执行时，此节点将为特定的“Target”Actor 新建一个 DecalComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 DecalComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:DecalComponent]

[EXCERPT:SpringArmComponent]

SpringArmComponents 用于尝试保持子项到父项之间的固定距离。如出现碰撞则缩回子项，不出现碰撞则回弹。举例而言，它可用作“摄像机升降臂”，防止角色的跟随摄像机坠入世界场景中。

执行时，此节点将为特定的“Target”Actor 新建一个 SpringArmComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 SpringArmComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:SpringArmComponent]

[EXCERPT:BillboardComponent]

UBillboardComponent 代表一个 2D 材质，此材质被渲染时将固定朝向摄像机。执行时，此节点将为特定的“Target”Actor 新建一个 UBillboardComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 UBillboardComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:BillboardComponent]

[EXCERPT:StaticMeshComponent]

StaticMeshComponent 用于静止的模型。执行时，此节点将为特定的“Target”Actor 新建一个 StaticMeshComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 StaticMeshComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:StaticMeshComponent]

[EXCERPT:TextRenderComponent]

TextRenderComponents 以给定的字体渲染世界场景中的文本。此组件包含字体相关的常用属性，如 Scale、Alignment、Color 等。 

执行时，此节点将为特定的“Target”Actor 新建一个 TextRenderComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 TextRenderComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:TextRenderComponent]

[EXCERPT:VectorFieldComponent]

执行时，此节点将为特定的“Target”Actor 新建一个 VectorFieldComponent。  

如“Manual Attachment”输入未设置（False），则新组件将自动被附加到 Actor 的根下（如没有根，则创建一个根）。如“Manual Attachment”输入已设置（True），则新组件不会被附加。

新建的 VectorFieldComponent 通过“Return Value”输出返回用户。如“Manual Attachment”已选中，则用户可选取并自行进行附加（通过“Attach to”节点）。

“Relative Transform”输入定义组件相对于其父类的位置和方向。如未附加，此变形则相对于世界场景。
[/EXCERPT:VectorFieldComponent]

