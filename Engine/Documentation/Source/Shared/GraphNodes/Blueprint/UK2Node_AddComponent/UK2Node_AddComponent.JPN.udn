INTSourceChangelist:2573972
Availability:NoPublish
Title:UK2Node_AddComponent
Crumbs:
Description: ブループリント "Add Component" ノードの概要

[EXCERPT:ArrowComponent]

ArrowComponent は線を使用してレンダリングされる簡易な矢印です (オブジェクトが対面している方向を示すのに便利です)。実行するとこのノードは指定された 'Target' アクタに対して新規 ArrowComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された ArrowComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:ArrowComponent]

[EXCERPT:AtmosphericFogComponent]

AtmosphericFogComponents は、フォグエフェクト (雲など) の作成に使用します。実行するとこのノードは指定された 'Target' アクタに対して新規 AtmosphericFogComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された AtmosphericFogComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:AtmosphericFogComponent]

[EXCERPT:AudioComponent]

AudioComponents は、オブジェクトにオーディオを付けて提供するために使用します。実行するとこのノードは指定された 'Target' アクタに対して新規 AudioComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された AudioComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:AudioComponent]

[EXCERPT:BoxComponent]

BoxComponent は線を使用してレンダリングされる単純なボックス形状です (一般的に単純なコリジョンで使用されます)。実行するとこのノードは指定された 'Target' アクタに対して新規 BoxComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された BoxComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:BoxComponent]

[EXCERPT:CapsuleComponent]

CapsuleComponent は線を使用してレンダリングされる単純なカプセル形状です (一般的に単純なコリジョンで使用されます)。実行するとこのノードは指定された 'Target' アクタに対して新規 CapsuleComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された CapsuleComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:CapsuleComponent]

[EXCERPT:CustomMeshComponent]

CustomMeshComponent は、カスタム仕様のトライアングル メッシュのジオメトリ生成を可能にします。実行するとこのノードは指定された 'Target' アクタに対して新規 CustomMeshComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された CustomMeshComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:CustomMeshComponent]

[EXCERPT:DecalComponent]

DecalComponent はメッシュのサーフェス上にレンダリングするマテリアルです (モデルに対してバンパースティッカーのような役割を果たします)。実行するとこのノードは指定された 'Target' アクタに対して新規 DecalComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された DecalComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:DecalComponent]

[EXCERPT:DestructibleComponent]

DestructibleComponent は、DestructibleActor の物理データを保持します。実行するとこのノードは指定された 'Target' アクタに対して新規 DestructibleComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された DestructibleComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:DestructibleComponent]

[EXCERPT:DirectionalLightComponent]

DirectionalLightComponent は太陽のような光源として機能します。light-mass importance ボリュームで任意のサーフェスに対して均一なライティングを行います。実行するとこのノードは指定された 'Target' アクタに対して新規 DirectionalLightComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された DirectionalLightComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:DirectionalLightComponent]

[EXCERPT:ExponentialHeightFogComponent]

ExponentialHeightFogComponent は、AtmosphericFogComponent に似ていますが (雲などフォグエフェクトの作成に使用)、フォグの高さに関連する密度も伴います。実行するとこのノードは指定された 'Target' アクタに対して新規 ExponentialHeightFogComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された ExponentialHeightFogComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:ExponentialHeightFogComponent]

[EXCERPT:InstancedStaticMeshComponent]

InstancedStaticMeshComponent は、スタティックメッシュで複数のインスタンスを持つことができます。実行するとこのノードは指定された 'Target' アクタに対して新規 InstancedStaticMeshComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された InstancedStaticMeshComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:InstancedStaticMeshComponent]

[EXCERPT:MaterialBillboardComponent]

UMaterialBillboardComponent は、常にカメラに向いてレンダリングされる 2D マテリアルです実行するとこのノードは指定された 'Target' アクタに対して新規 UMaterialBillboardComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された UMaterialBillboardComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:MaterialBillboardComponent]

[EXCERPT:ParticleSystemComponent]

ParticleSystemComponent はパーティクルエミッタとして機能します。実行するとこのノードは指定された 'Target' アクタに対して新規 ParticleSystemComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された ParticleSystemComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:ParticleSystemComponent]

[EXCERPT:PawnNoiseEmitterComponent]

PawnNoiseEmitterComponent は、PawnSensingComponents が使用するノイズ イベント データを追跡します。このコンポーネントは Pawn またはその Controller のいずれかに存在するようになっています。ネットワーク クライアントに対しては何も行いません。 

実行するとこのノードは指定された 'Target' アクタに対して新規 PawnNoiseEmitterComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された PawnNoiseEmitterComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:PawnNoiseEmitterComponent]

[EXCERPT:PawnSensingComponent]

PawnSensingComponent はアクタに対して知覚 (すなわち、視覚と聴覚) 設定と機能をカプセル化します。これにより、アクタはワールド内のアクタを見たり、聴いたりすることができます。ネットワーク クライアントに対しては何も行いません。 

実行するとこのノードは指定された 'Target' アクタに対して新規 PawnSensingComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された PawnSensingComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:PawnSensingComponent]

[EXCERPT:PhysicsConstraintComponent]

PhysicsConstraintComponent は、2 つの剛体の接続に効果的なジョイントです。コンポーネントの様々なパラメータを使用して種類の異なるジョイントを作成できます。

実行するとこのノードは指定された 'Target' アクタに対して新規 PhysicsConstraintComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された PhysicsConstraintComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:PhysicsConstraintComponent]

[EXCERPT:PhysicsHandleComponent]

PhysicsHandleComponents は、物理アクタを移動させるユーティリティ オブジェクトです。実行するとこのノードは指定された 'Target' アクタに対して新規 PhysicsHandleComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された PhysicsHandleComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:PhysicsHandleComponent]

[EXCERPT:PhysicsThrusterComponent]

PhysicsThrusterComponents は、物理アクタを移動させるユーティリティ オブジェクトです。実行するとこのノードは指定された 'Target' アクタに対して新規 PhysicsThrusterComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された PhysicsThrusterComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:PhysicsThrusterComponent]

[EXCERPT:PointLightComponent]

PointLightComponents は、一点から全方向へ均一にライトをエミットするライトコンポーネントです。実行するとこのノードは指定された 'Target' アクタに対して新規 PointLightComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された PointLightComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:PointLightComponent]

[EXCERPT:PoseableMeshComponent]

PoseableMeshComponent は、ブループリントによるボーン変換の操作を可能にします。実行するとこのノードは指定された 'Target' アクタに対して新規 PoseableMeshComponent  を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された PoseableMeshComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:PoseableMeshComponent]

[EXCERPT:ProjectileMovementComponent]

ProjectileMovementComponents は、ティック時の関連コンポーネントの位置を更新します。更新されたコンポーネントが物理をシミュレート中の場合は、初回起動パラメータ（初回ベロシティがノンゼロのとき）のみがプロジェクタイルに影響を及ぼします。そこから物理シミュレーションが引き継がれます。

実行するとこのノードは指定された 'Target' アクタに対して新規 ProjectileMovementComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された ProjectileMovementComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:ProjectileMovementComponent]

[EXCERPT:RadialForceComponent]

RadialForceComponents は物理オブジェクトおよび / または被破壊性オブジェクトに影響を与えるラジアルフォース / 衝撃の放射に使用します。実行するとこのノードは指定された 'Target' アクタに対して新規 RadialForceComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された RadialForceComponents は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:RadialForceComponent]

[EXCERPT:RotatingMovementComponent]

RotatingMovementComponents は、各ティック毎に関連する PrimitiveComponent の位置を更新するために使用されます。実行するとこのノードは指定された 'Target' アクタに対して新規 RotatingMovementComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された RotatingMovementComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:RotatingMovementComponent]

[EXCERPT:SceneCaptureComponent2D]

SceneCaptureComponent2D は、単一面からシーンの「スナップショット」をキャプチャして、これらをレンダー ターゲットへ入力するために使用します。 

実行するとこのノードは指定された 'Target' アクタに対して新規 SceneCaptureComponent2D を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された SceneCaptureComponent2D は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:SceneCaptureComponent2D]

[EXCERPT:SceneCaptureComponentCube]

SceneCaptureComponentCube は、6 つの面からシーンの「スナップショット」をキャプチャして、これらをレンダーターゲットへ入力するために使用します。 

実行するとこのノードは指定された 'Target' アクタに対して新規 SceneCaptureComponentCube を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された SceneCaptureComponentCube は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:SceneCaptureComponentCube]

[EXCERPT:SceneComponent]

実行するとこのノードは指定された 'Target' アクタに対して新規 SceneComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された SceneComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:SceneComponent]

[EXCERPT:SkeletalMeshComponent]

実行するとこのノードは指定された 'Target' アクタに対して新規 SkeletalMeshComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された SkeletalMeshComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:SkeletalMeshComponent]

[EXCERPT:SphereComponent]

SphereComponent は線を使用してレンダリングされる単純な球体形状です (一般的に単純なコリジョンで使用されます)。実行するとこのノードは指定された 'Target' アクタに対して新規 SphereComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された SphereComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:SphereComponent]

[EXCERPT:SpotLightComponent]

SpotLightComponent は、指向性コーン形状 (たいまつ状) のライトをエミットします。実行するとこのノードは指定された 'Target' アクタに対して新規 SpotLightComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された SpotLightComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:SpotLightComponent]

[EXCERPT:DecalComponent]

DecalComponent は、メッシュのサーフェス上にレンダリングするマテリアルです (モデルに対してバンパースティッカーのような役割を果たします)。実行するとこのノードは指定された 'Target' アクタに対して新規 DecalComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された DecalComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:DecalComponent]

[EXCERPT:SpringArmComponent]

SpringArmComponents は親から固定距離で子供たちを保持しようと試みますが、コリジョンがあった場合は子供たちを取り消して、コリジョンがない場合は復活します。例えば、プレイヤーの追跡カメラがワールドと衝突しないように「カメラブーム」として使用します。

実行するとこのノードは指定された 'Target' アクタに対して新規 SpringArmComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された SpringArmComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:SpringArmComponent]

[EXCERPT:BillboardComponent]

UBillboardComponent は、常にカメラに向いてレンダリングされる 2D テクスチャです。実行するとこのノードは指定された 'Target' アクタに対して新規 UBillboardComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された UBillboardComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:BillboardComponent]

[EXCERPT:StaticMeshComponent]

StaticMeshComponent はアニメートしないメッシュに使用します。実行するとこのノードは指定された 'Target' アクタに対して新規 StaticMeshComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された StaticMeshComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:StaticMeshComponent]

[EXCERPT:TextRenderComponent]

TextRenderComponents は任意のフォントでワールドにテキストをレンダリングします。このコンポーネントには、スケール、アラインメント、カラーなどの通常のフォント関連の属性が含まれます。 

実行するとこのノードは指定された 'Target' アクタに対して新規 TextRenderComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された TextRenderComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:TextRenderComponent]

[EXCERPT:VectorFieldComponent]

実行するとこのノードは指定された 'Target' アクタに対して新規 VectorFieldComponent を作成します。  

'Manual Attachment' 入力が設定されないままになっていると (False)、アクタのルートに新規コンポーネントが自動的にアタッチされます (または、これまでに存在していなければルートを作成します)。'Manual Attachment' 入力が設定されていると (True)、新規コンポーネントはアタッチされないままになります。

新規作成された VectorFieldComponent は 'Return Value' 出力を介してユーザーに戻されます。'Manual Attachment' が選択されていると、ユーザーはそれを使用し、自分でアタッチすることができます ("Attach to" ノードを介して)。

'Relative Transform' 入力では親に相対的にどこにコンポーネントを配置し、方向付けするかを定義します。アタッチしないままにすると、この変換はワールドに対して相対的になります。
[/EXCERPT:VectorFieldComponent]

