INTSourceChangelist:3262227
Availability:Public
Title:3 - 从 IDE 编译 UE4
Crumbs:
Description:学习如何从 IDE 编译虚幻引擎。
Version:4.14
type:how to
parent:Platforms/Linux/BeginnerLinuxDeveloper/SettingUpAnIDE
order:3
tags:Linux
SkillLevel:Beginner

[NAV]

配置 Qt Creator 编译并运行 UE4 后，即可从 IDE 运行 UE4。从 IDE 运行 UE4 之前，需要打开一些输出窗口，以便看到工作中的虚幻编译系统。

1.	点击 **Edit** 按钮进入 Edit 模式，打开 Qt Creator 编辑器。

	[REGION:lightbox]
		[![](SetupIDE_Step15.png)(w:720)](SetupIDE_Step15.png)
	[/REGION]
	
	[REGION:caption]
		点击查看全图。
	[/REGION]

	[REGION:note]
	可在编辑模式中查看、调试和编辑助力 UE4的 CPP 源代码。
	[/REGION]

1.	Qt Creator 默认隐藏主工具栏。将鼠标指针悬停在屏幕顶部，触及顶部边缘即可显示工具栏。

	![](SetupIDE_Step15A.png)

1.	打开 **Window** 菜单，鼠标悬停在 **Output Panes** 子菜单上，点击 **Compile Output**。此外，也可按下 **Alt+4** 组合键打开 **Compile Output** 窗口面板。

	![](SetupIDE_Step15B.png)

	[REGION:note]
	从源代码编译 UE4 时打开 **Compile Output** 窗口面板可了解是否出现错误。
	[/REGION]

## 编译 UE4
	
1.	将鼠标指针悬停在屏幕顶部，触及顶部边缘即可显示工具栏。

	![](SetupIDE_Step15A.png)

1.	如需编译 UE4，打开 **Build** 菜单并点击 **Build Project "UE4”** 按钮。此外，按下 **Ctrl+B** 组合键也可编译 UE4。

	![](SetupIDE_Step15C.png)

## 最终结果

[REGION:lightbox]
	[![](SetupIDE_Step16.png)(w:720)](SetupIDE_Step16.png)
[/REGION]

[REGION:caption]
	点击查看全图。
[/REGION]

此时您便会看到 IDE 中编译器刷新消息到 **Compile Output** 窗口。 
编译引擎的时间从四分钟到一个多小时不等，取决于系统配置。 
如果希望缩短从源代码编译引擎的时间，建议使用至少拥有 8G RAM 和 8 核处理器（包括超线程）的电脑进行源代码编译。

[NAV]
