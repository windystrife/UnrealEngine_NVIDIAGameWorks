Availability:Public
Title:按键绑定编辑器
Crumbs:%ROOT%, Engine, Engine/UI
Description:虚幻编辑器中创建命令的可配置控制。

[EXCERPT:Intro]
**Keybindings Editor(按键绑定编辑器)** 用于把按键组合同一个动作或命令关联到一起。
在虚幻编辑器中,常见命令及这些针对主机或其他应用程序的特定命令都是可配置的。
每个用户都可以设定他们自己的一组自定义按键绑定来适应他们的工作流程和个人喜好。
[/EXCERPT:Intro]

![Key Bindings App](key_bindings.png)

**Search(搜索)** 文本框允许您通过输入部分命令名称或整个命令名称来过滤已经显示的命令列表。

![Key Bindings Filter](key_bindings_search.png)

## 绑定新命令

1. 单击您想绑定的命令对应的文本域。

1. 按下您想使用的用于执行该命令的按键组合。 

	![Key Bindings - Set](key_bindings_set.png)

1. 点击 ![Key Bindings - Accept](button_accept.png) 来设置按键绑定。

	![Key Bindings - Finished](key_bindings_final.png)

	
[REGION:tip]
如果您按下的按键组合已经和其他命令相绑定，那么将会发出警告并提示如果需要可以
覆盖那个按键绑定。
	
![Key Bindings - Override](key_bindings_existing.png)(w:720 convert:false)

点击 ![Key Bindings - Override](button_override.png) 按钮来为当前命令设置按键绑定并
删除当前绑定到那组按键的命令中的按键绑定。
[/REGION]

## 删除已有按键绑定

1. 点击您想取消绑定的命令的当前按键绑定旁边的 ![Key Bindings - Delete](button_delete.png) 按钮。

	![Key Bindings - Remove](key_bindings_remove.png)

1. 该命令不再绑定到任何按键。

	![Key Bindings - Unbound](key_bindings_none.png)
