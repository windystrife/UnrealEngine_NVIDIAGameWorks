INTSourceChangelist:1649879
Title: 数组控件
Crumbs: %ROOT%, Engine, Engine/UI, Engine/UI/LevelEditor, Engine/UI/LevelEditor/Details
Description:其他属性类型的动态列表。
Availability:Public

`Array(数组)`是其他类型属性的动态列表，比如对象引用属性。数组的元素列表垂直显示每个独立的属性元素，每种类型的属性有其对应的标准控件 
。

![Array control](array.png)

通过点击顶部的 ![Add Item](button_array_new.png) 按钮可以添加元素项。

![Array with new element](array_new.png)

通过按下  ![Clear Array](button_array_empty.png)  按钮可以删除数组中的所有现有元素。

![Array with no elements](array_empty.png)

通过按下 ![Element Options](button_array_element_options.png)  按钮，数组中的每个元素都可以执行所呈现的其中一个功能选项。

![Element Options Menu](array_element_options_menu.png)

| 选项 | 描述|
| ------ | ----------- |
| **Insert(插入)** |在当前元素前面向数组中插入一个新的元素。[INCLUDE:#ArrayInsert] |
| **Delete（删除）** | 删除当前元素。[INCLUDE:#ArrayDelete] |
| **Duplicate（副本）** | 在当前元素后面向数组插入该元素的一个副本。[INCLUDE:#ArrayDuplicate] |

<!--
[EXCERPT:ArrayInsert]
[REGION:imagetable]
| ![](array.png) | ![Array with inserted element](array_insert.png) |
| ------------ | ----------- |
| 插入元素之前| 插入元素之后|
[/REGION]
[/EXCERPT:ArrayInsert]

[EXCERPT:ArrayDelete]
[REGION:imagetable]
| ![](array.png) | ![Array with deleted element](array_delete.png) |
| ------------ | ----------- |
| 删除元素之前 | 删除元素之后|
[/REGION]
[/EXCERPT:ArrayDelete]

[EXCERPT:ArrayDuplicate]
[REGION:imagetable]
| ![](array.png) | ![Array with copied element](array_duplicate.png) |
| ------------ | ----------- |
| 复制元素之前 | 复制元素之后|
[/REGION]
[/EXCERPT:ArrayDuplicate]
-->
