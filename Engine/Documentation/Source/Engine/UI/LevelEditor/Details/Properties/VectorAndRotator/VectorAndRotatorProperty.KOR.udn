INTSourceChangelist:3108692
Availability:Public
Title: 벡터 / 로테이터 콘트롤
Crumbs: %ROOT%, Engine, Engine/UI, Engine/UI/LevelEditor, Engine/UI/LevelEditor/Details
Description:여러 개의 Float 값이 들어있는 Vector 또는 Rotator 프로퍼티 입니다.
Version: 4.9

 `Vector` 와 `Rotator` 는 가로 배치된 해당 성분 각각에 대한 숫자 입력값을 표시해 주는 다중 프로퍼티
입니다.

## 벡터

 `Vector` 는 공간 내 위치나 양 등, 세 개의 `Float` 값을 통해 이상적으로 표현되는 복합 데이터 
저장을 위한 프로퍼티입니다.

 `Vector` 프로퍼티 내 각각의 `Float` 값, 즉 **X**, **Y**, **Z** 는 숫자 입력 칸으로 표시되며, 
값을 키보드에서 직접 입력받거나 
**마우스 왼쪽 버튼** 으로 클릭하고 가로로 끌어 각각의 값을 빠르게 변경할 수 있습니다.

![Vector control](vector.png)

## 로테이터

 `Rotator` 는 `[0.0, 360.0]` 범위로 제한되는 세 개의 `Float` 값을 통해서 회전값을 저장하기 위해 특별히 
고안된 프로퍼티로, 각각의 축에 대해 얼마만큼 회전할 것인가를 나타냅니다.

`Rotator` 각각의 값 **X**, **Y**, **Z** 는 **Pitch**, **Roll**, **Yaw** 값에 매핑되며, 숫자 회전판으로 표시됩니다.
키보드에서 숫자값을 바로 입력할 수도 있고, **마우스 왼쪽 버튼** 을 클릭하고 좌우로 끌어 
각각의 값을 빠르게 수정할 수도 있습니다.

![Rotator control](rotator.png)
