INTSourceChangelist:3244370
Availability:Public
Title:Timeline 노드
Crumbs: %ROOT%, Engine, Engine/Blueprints, Engine/Blueprints/UserGuide, Engine/Blueprints/UserGuide/Timelines
Description: 블루프린트 타임라인의 특정 함수성 처리에 사용되는 노드입니다.
version: 4.9
tags:Blueprints

[VAR:Topic]
[OBJECT:Topic]
	[PARAM:image]
		![%Engine\Blueprints\UserGuide\Timelines\TimelineNodes:title%](Engine\Blueprints\UserGuide\Timelines.png)
	[/PARAM]
	[PARAM:icon]
	[/PARAM]
	[PARAM:title]
		%Engine\Blueprints\UserGuide\Timelines\TimelineNodes:title%
	[/PARAM]
	[PARAM:description]
		%Engine\Blueprints\UserGuide\Timelines\TimelineNodes:description%
	[/PARAM]
	[PARAM:path]
		[RELATIVE:Engine\Blueprints\UserGuide\Timelines\TimelineNodes]
	[/PARAM]
[/OBJECT]
[/VAR]

[VAR:TopicCompact]
[OBJECT:TopicCompact]
	[PARAM:image]
		![%Engine\Blueprints\UserGuide\Timelines\TimelineNodes:title%](Engine\Blueprints\UserGuide\Timelines.png)
	[/PARAM]
	[PARAM:icon]
	[/PARAM]
	[PARAM:title]
		%Engine\Blueprints\UserGuide\Timelines\TimelineNodes:title%
	[/PARAM]
	[PARAM:description]
		%Engine\Blueprints\UserGuide\Timelines\TimelineNodes:description%
	[/PARAM]
	[PARAM:path]
		[RELATIVE:Engine\Blueprints\UserGuide\Timelines\TimelineNodes]
	[/PARAM]
[/OBJECT]
[/VAR]



## Get Play Rate

**Get Play Rate** (재생 속도 구하기) 노드는 입력 타임라인의 현재 재생 속도를 반환합니다. 반환되는 수치는 실수 (float) 값입니다.

![](Get_PlayRate.png)

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
|[REGION:tablesection] **출력** [/REGION] |||
| **Return Value** | Float | 타임라인의 현재 재생 속도를 출력합니다. |

## Get Playback Position

**Get Playback Position** (재생 위치 구하기) 노드는 현재 재생 위치, 또는 입력 타임라인 상에서 시간적으로 현재 위치를 반환합니다. 반환되는 수치는 실수 값입니다.

![](Get_Playback_Position.png)

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
|[REGION:tablesection] **출력** [/REGION] |||
| **Return Value** | Float | 타임라인의 현재 재생 위치를 출력합니다. |

## Get Timeline Length

**Get Timeline Length** (타임라인 길이 구하기) 노드는 입력 타임라인의 총 길이를 실수 값으로 반환합니다.

![](Get_Timeline_Length.png)

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
|[REGION:tablesection] **출력** [/REGION] |||
| **Return Value** | Float | 타임라인의 총 재생 길이를 출력합니다. |

## Is Looping

**Is Looping** (반복중인지) 노드는 입력 타임라인이 반복중이면 _True_, 그렇지 않은 경우 _False_ 불리언 값을 반환합니다.

![](Is_Looping.png)

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
|[REGION:tablesection] **출력** [/REGION] |||
| **Return Value** | Boolean | Looping 프로퍼티 값을 출력합니다. |

## Is Playing

**Is Playing** (재생중인지) 노드는 입력 타임라인이 현재 재생중이면 _True_, 아니면 _False_ 불리언 값을 반환합니다.

![](Is_Playing.png)

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
|[REGION:tablesection] **출력** [/REGION] |||
| **Return Value** | Boolean | 타임라인이 현재 재생중인지를 출력합니다. |

## Is Reversing

**Is Reversing** (역재생중인지) 노드는 타임라인이 현재 역재생중이면 _True_, 아니면 _False_ 불리언 값을 반환합니다.

![](Is_Reversing.png)

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
|[REGION:tablesection] **출력** [/REGION] |||
| **Return Value** | Boolean | 타임라인이 현재 역재생중인지를 출력합니다. |

## Play

**Play** (재생) 노드는 입력 타임라인을 현재 재생 위치에서부터 재생 시작하라 이릅니다.

![](Play01.png)

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |


## Play from Start

![](Play_From_Start.png)

**Play from Start** (시작부터 재생) 노드는 입력 타임라인을 타임라인 시작서부터 재생 시작하라 이릅니다.

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |

## Reverse

![](Reverse01.png)

**Reverse** (역재생) 노드는 입력 타임라인을 현재 재생 위치서부터 역재생하라 이릅니다.

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |

## Reverse from End

![](Reverse_From_End.png)

**Reverse from End** (끝에서부터 역재생) 노드는 입력 타임라인을 끝에서부터 역재생하라 이릅니다.

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |

## Set Looping

![](Set_Looping.png)

**Set Looping** (루핑 설정) 노드는 입력 타임라인과 불리언 값을 받은 다음, 타임라인의 Looping 프로퍼티를 불리언 값으로 설정합니다.

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
| **New Looping** | Boolean | 타임라인의 루핑 설정에 사용됩니다. |

## Set New Time

![](Set_New_Time.png)

**Set New Time** (새로운 시간 설정) 노드는 입력 타임라인과 실수 값을 받은 다음, 타임라인의 New Time 프로퍼티를 실수 값으로 설정합니다.

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
| **New Time** | Float | 새로운 재생 위치 설정에 사용됩니다. |

## Set Play Rate

![](Set_Play_Rate.png)

**Set Play Rate** (재생 속도 설정) 노드는 입력 타임라인과 실수 값을 받은 다음, 타임라인의 Play Rate 프로퍼티를 실수 값으로 설정합니다.

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
| **New Rate** | Float | 새로운 재생 속도 설정에 사용됩니다. |

## Set Playback Position

![](Set_Playback_Position.png)

**Set Playback Position** (재생 위치 설정) 노드는 입력 타임라인, 실수 값, 불리언 값을 받은 다음, 타임라인의 Playback Position 프로퍼티를 실수 값으로 설정하여 점프시킵니다. 타임라인 안에 이벤트 트랙이 있는 경우, 불리언 값을 사용하여 발동 여부를 결정합니다.

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
| **New Position** | Float | 새로운 재생 위치로 점프하는 데 사용됩니다. |
| **Fire Events** | Boolean | (이벤트 트랙에서의) 이벤트 발동 생략 여부를 결정하는 데 사용됩니다. |

## Set Timeline Length

![](Set_Timeline_Length.png)

**Set Timeline Length** (타임라인 길이 설정) 노드는 타임라인의 총 재생 길이를 설정합니다.

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
| **New Length** | Float | 새로운 재생 길이를 설정하는 데 사용됩니다. |

## Set Timeline Length Mode

![](Set_Timeline_LengthMode.png)

**Set Timeline Length Mode** (타임라인 길이 모드 설정) 노드는 타임라인의 Length Mode 프로퍼티를 설정합니다. 이를 통해 전체 타임라인 길이 재생과 지난 키프레임을 재생 사이를 전환할 수 있습니다.

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |
| **New Length Mode** | Byte | 재생 모드를 "Timeline Length" (타임라인 길이) 또는 "Last Keyframe" (지난 키프레임) 으로 설정합니다. |

## Stop

![](Stop01.png)

**Stop** (중지) 노드는 입력 타임라인 노드의 재생을 중지합니다.

| **이름** | **유형** | **설명** |
| --- | --- |---|
|[REGION:tablesection] **입력** [/REGION] |||
| **Target** | Timeline Component | 타임라인 변수를 받습니다. |

