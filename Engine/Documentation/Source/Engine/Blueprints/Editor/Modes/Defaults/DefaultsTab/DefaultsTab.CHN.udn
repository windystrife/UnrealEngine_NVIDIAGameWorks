INTSourceChangelist:2085818
Availability:Public
Title: 蓝图编辑器默认值选卡
Crumbs:%ROOT%, Engine/Blueprints, Engine/Blueprints/Editor, Engine/Blueprints/Editor/Modes, Engine/Blueprints/Editor/Modes/Defaults
Description:关于蓝图编辑器默认值选卡的概述。

[TOC(start:2)]

## 概述

 **Blueprint Defaults（蓝图编辑器默认值）** 包含了关于蓝图的默认设置、属性及蓝图中包含的所有变量的信息。您可以修改这些设置来满足您自己的需要。

![](BP_DefaultsTab.png)

**蓝图默认值** 选卡包含以下这些部分：

| 项目| 描述|
| ---- | ----------- |
| **Default（默认）** | 如果您在创建某个变量时没有指定除 **Default** 之外的其他类目，那么这里则列出所有变量及它们的值。注意您必须编译该蓝图，才能在 **Blueprint Defaults(蓝图默认值)** 选卡中显示它的变量。|
| **Rendering（渲染）** | 包含了和如何在游戏中渲染基于蓝图的Actor的相关属性的信息。|
| **Replication(复制)** | 包含了指示您的actor如何在网络游戏中表现的属性相关的信息，比如它的网络优先级及同其他客户端的关联。|
| **Input（输入）** | 包含了基于该蓝图的actor如何对输入做出反应的相关信息。|
| **Actor** | 包含了基于这个蓝图的Actor相关的信息，比如它们是否会受到伤害。注意，对于关卡蓝图来说，在这部分中仅 **Tags（标签）** 属性是有用的。|


[REGION:note]
如果您为变量创建自定义的类目，那么在编译完蓝图后 **蓝图默认值** 选卡也会显示这些类目。
[/REGION]

关于 **Defaults（默认）** 模式的更多信息，请参照 [](Engine/Blueprints\Editor\Modes\Defaults)。

[INCLUDE:Engine/Blueprints/Editor/Modes#modes]


